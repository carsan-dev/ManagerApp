Para ejecutar el proyecto en android:

- npm start -- --reset-cache
- npm run android

Para matar el proceso si se queda pillado:

- npx kill-port 8081

Para compilar en .exe:

- cd android/
- ./gradlew clean
- cd ..
- npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res
- cd android/
- ./gradlew assembleDebug

 Procedimiento para cambiar de versión:

  1. Subir versión en package.json (ej: "version": "1.0.1")
  2. Actualizar android/app/build.gradle (
    versionCode 2
    versionName "1.0.1"
  )
  3. Crear APK: cd android && ./gradlew clean && ./gradlew assembleRelease (  El APK se genera en android/app/build/outputs/apk/release/app-release.apk)
  4. Subir release a GitHub con etiqueta de versión y el fichero como asset
  5. Copiar el link de descarga (https://github.com/carsan-dev/ManagerApp/releases/download/v3.0.1/SportsManager_3.0.1.apk)
  6. Actualizar Firebase Remote Config:
    - latest_version: 1.0.1
    - update_url: el link directo
    - force_update: true/false según quieras forzar
    - update_message: mensaje opcional
  7. Publicar cambios en Firebase Console